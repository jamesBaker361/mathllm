import unittest

class DataLoadTest(unittest.TestCase):
    def test_get_dataset(self):
        '''
        when given the tasks (multiplication, division, etc) and a number types (whole, decimal)
        returns the train, test datasets
        '''
        return
